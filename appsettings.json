{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "DataSettings": {
    "DataViewerConnStr": "Server=(local);Database=AdventureWorks;Trusted_Connection=True;",
    "RDBMS": "SQL" // SQL (for SQL SERVER) or MYSQL
  }
  
}
